# restaurant-voice-order-saas

SaaS MVP for restaurant voice ordering, built with Spec-Driven Development and an AI harness.

## Product intent

The product enables restaurant waiters to speak orders into an Android app, review the structured draft generated by STT/parser components, edit or reject it when needed, and only then submit confirmed order items to kitchen/bar screens in real time.

The core safety rule is mandatory: AI-generated or AI-parsed orders are drafts only and must never be submitted, persisted as submitted, or emitted to kitchen/bar screens before explicit human waiter confirmation.

## Selected MVP stack

- Android waiter app: Flutter.
- Web admin/kitchen/bar: Flutter Web for MVP.
- Backend API: Python FastAPI.
- Database: PostgreSQL.
- Realtime: authenticated WebSocket.
- AI/voice: STT and parser adapters behind application interfaces.

## Monorepo structure

```text
apps/
  waiter_app/       # Flutter Android waiter app, skeleton only in T-001
  web_console/      # Flutter Web admin + kitchen/bar console, skeleton only in T-001
services/
  api/              # Python FastAPI backend, skeleton only in T-001
packages/
  api_contracts/    # Shared API contract artifacts, skeleton only in T-001
infra/              # Local infrastructure, skeleton only in T-001
```

## Current implementation state

This repository currently contains documentation and the initial monorepo folder skeleton. TASK T-001 intentionally does not add runnable backend, Flutter, database, Docker Compose or product behavior.

## Local development intent

Future tasks will add the runnable pieces incrementally:

1. FastAPI health endpoint and backend test runner.
2. Docker Compose with PostgreSQL.
3. Migrations and base schema.
4. Auth, CRUD, order lifecycle, realtime, voice draft and confirmation workflows.
5. Flutter Android and Flutter Web clients.
6. Demo seed data and final validation flow.

Until those tasks are completed, validation is limited to repository text/diff checks and verifying the expected skeleton files exist.

## Working with this repository

Read `AGENTS.md` and the SDD files before implementation. Follow `TASKS.md` sequentially and execute only the next pending task unless explicitly instructed otherwise.
